"""App configuration for Pods application."""

from django.apps import AppConfig


class PodsAppConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'pods_app'
    verbose_name = 'Pods - Visual Idea Organization'
